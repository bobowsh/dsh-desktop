import { AgentId, type AgentRecord, type SquadExecutionPlan, type SquadMemberHandoff, type SquadPlanAssignment, type SquadRecord } from '../types.ts';
export interface PlanValidationOptions {
    readonly requireAllMembers: boolean;
    readonly allowSkip: boolean;
}
/** Validate identities and dependencies, then produce a deterministic topological display order. */
export declare function validateExecutionPlan(plan: SquadExecutionPlan, squad: SquadRecord, options: PlanValidationOptions): SquadExecutionPlan;
/** Role-scoped fallback used when a planner route fails or emits an invalid/cyclic graph. */
export declare function deterministicExecutionPlan(squad: SquadRecord, task: string, agents: ReadonlyMap<AgentId, AgentRecord>, warning?: string, effectiveExecutionMode?: 'serial' | 'parallel'): SquadExecutionPlan;
/** Dependency-ready waves. Nodes in one wave may execute concurrently. */
export declare function executionWaves(assignments: readonly SquadPlanAssignment[]): SquadPlanAssignment[][];
/** Keep model-facing handoffs bounded even when a member returns a very large artifact or log. */
export declare function normalizeHandoff(value: unknown, fallbackText: string): SquadMemberHandoff;
//# sourceMappingURL=orchestration.d.ts.map