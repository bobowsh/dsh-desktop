import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { StateDot } from '@deepseek-ai/dsh-client-ui-primitives';
import styles from './loading-indicator.module.css';
/**
 * The pixel-chase loading indicator.
 * @param props - size, optional label, extra class.
 * @returns the indicator element.
 */
export function LoadingIndicator(props) {
    const { size = 12, label, className } = props;
    return (_jsxs("span", { className: className === undefined ? styles.indicator : `${styles.indicator} ${className}`, children: [_jsx(StateDot, { state: "ongoing", size: size }), label !== undefined && _jsx("span", { className: styles.label, children: label })] }));
}
