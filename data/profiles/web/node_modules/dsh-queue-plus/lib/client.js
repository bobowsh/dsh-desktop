window.__ModuleLoader__.load({
	id: "dsh-queue-plus",
	factory: (require) => {
		var module = { exports: {} };
		var exports = module.exports;
		Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
		let react = require("react");
		let _deepseek_ai_dsh_client_ui_primitives = require("@deepseek-ai/dsh-client-ui-primitives");
		let react_jsx_runtime = require("react/jsx-runtime");
		//#region src/shared.ts
		/** Same-origin endpoint owned by the Queue Plus host plugin. */
		const QUEUE_PLUS_PATH = "/queue-plus";
		//#endregion
		//#region src/client/api.ts
		var QueuePlusClientError = class extends Error {
			code;
			constructor(code, message) {
				super(message);
				this.code = code;
				this.name = "QueuePlusClientError";
			}
		};
		async function call(request) {
			const response = await fetch(QUEUE_PLUS_PATH, {
				method: "POST",
				headers: {
					accept: "application/json",
					"content-type": "application/json"
				},
				cache: "no-store",
				body: JSON.stringify(request)
			});
			const value = await response.json();
			if (!response.ok || value.ok !== true) {
				const failure = value;
				throw new QueuePlusClientError(failure.error?.code ?? `HTTP_${String(response.status)}`, failure.error?.message ?? "queue operation failed");
			}
			return value;
		}
		async function moveQueue(request) {
			const result = await call(request);
			if (result.action !== "move") throw new QueuePlusClientError("BAD_RESPONSE", "unexpected queue response");
			return result;
		}
		async function clearQueue(sessionId, expectedOrder) {
			const result = await call({
				action: "clear",
				sessionId,
				expectedOrder
			});
			if (result.action !== "clear") throw new QueuePlusClientError("BAD_RESPONSE", "unexpected queue response");
			return result;
		}
		async function undoClear(sessionId, token) {
			const result = await call({
				action: "undo",
				sessionId,
				token
			});
			if (result.action !== "undo") throw new QueuePlusClientError("BAD_RESPONSE", "unexpected queue response");
			return result;
		}
		//#endregion
		//#region src/client/styles.ts
		const styles = {
			dock: "dsh-queue-plus-dock",
			panel: "dsh-queue-plus-panel",
			header: "dsh-queue-plus-header",
			summaryButton: "dsh-queue-plus-summary-button",
			lead: "dsh-queue-plus-lead",
			count: "dsh-queue-plus-count",
			sortingLabel: "dsh-queue-plus-sorting-label",
			modeButton: "dsh-queue-plus-mode-button",
			collapseButton: "dsh-queue-plus-collapse-button",
			body: "dsh-queue-plus-body",
			list: "dsh-queue-plus-list",
			row: "dsh-queue-plus-row",
			index: "dsh-queue-plus-index",
			dragHandle: "dsh-queue-plus-drag-handle",
			gripDots: "dsh-queue-plus-grip-dots",
			preview: "dsh-queue-plus-preview",
			editor: "dsh-queue-plus-editor",
			actions: "dsh-queue-plus-actions",
			iconButton: "dsh-queue-plus-icon-button",
			inlineButton: "dsh-queue-plus-inline-button",
			dangerButton: "dsh-queue-plus-danger-button",
			footer: "dsh-queue-plus-footer",
			footnote: "dsh-queue-plus-footnote",
			clearButton: "dsh-queue-plus-clear-button",
			undo: "dsh-queue-plus-undo",
			undoText: "dsh-queue-plus-undo-text",
			undoButton: "dsh-queue-plus-undo-button",
			notice: "dsh-queue-plus-notice",
			srOnly: "dsh-queue-plus-sr-only"
		};
		const STYLE_ID = "dsh-queue-plus-style";
		const STYLE_REFS = Symbol.for("dsh.queuePlus.styleRefs");
		const STYLE_TEXT = String.raw`
.dsh-queue-plus-dock {
  box-sizing: border-box;
  flex: none;
  width: calc(100% - var(--dsh-composer-side-clearance) - var(--dsh-composer-side-clearance) - var(--dsh-composer-dock-inset) - var(--dsh-composer-dock-inset));
  max-width: calc(var(--dsh-composer-card-max-width) - var(--dsh-composer-dock-inset) - var(--dsh-composer-dock-inset));
  margin: 0 auto calc(0px - var(--dsh-composer-stack-gap) - 3px);
  padding: 0 var(--dsh-composer-dock-inset);
}

.dsh-queue-plus-panel {
  position: relative;
  width: 100%;
  overflow: hidden;
  padding: 2px 0;
  color: var(--dsw-alias-label-primary);
  background: var(--dsw-specific-tip);
  border-radius: 12px 12px 0 0;
  --dsh-scrollbar-thumb: var(--dsw-alias-scrollbar-bg-l2);
  --dsh-scrollbar-thumb-hover: var(--dsw-alias-scrollbar-hover-l2);
}

.dsh-queue-plus-panel::after {
  position: absolute;
  inset: 0;
  pointer-events: none;
  content: '';
  border: 1px solid var(--dsw-alias-border-l1);
  border-bottom: 0;
  border-radius: inherit;
}

.dsh-queue-plus-header {
  box-sizing: border-box;
  display: flex;
  align-items: center;
  min-height: 40px;
  gap: 4px;
  padding: 2px 6px 2px 4px;
}

.dsh-queue-plus-summary-button {
  display: flex;
  flex: 1 1 auto;
  align-items: center;
  align-self: stretch;
  min-width: 0;
  gap: 9px;
  padding: 4px 8px;
  color: inherit;
  font: inherit;
  text-align: start;
  cursor: pointer;
  background: transparent;
  border: 0;
  border-radius: 8px;
  touch-action: manipulation;
}

.dsh-queue-plus-summary-button:disabled {
  cursor: default;
}

.dsh-queue-plus-lead {
  display: grid;
  flex: none;
  color: var(--dsw-alias-label-tertiary);
  place-items: center;
}

.dsh-queue-plus-panel[data-sorting='true'] .dsh-queue-plus-lead {
  color: var(--dsw-alias-state-business-primary);
}

.dsh-queue-plus-count {
  min-width: 0;
  overflow: hidden;
  font-family: Inter, var(--dsw-font-family);
  font-size: 13px;
  font-weight: 500;
  line-height: 24px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.dsh-queue-plus-sorting-label {
  flex: none;
  color: var(--dsw-alias-state-business-primary);
  font-family: Inter, var(--dsw-font-family);
  font-size: 11px;
  font-weight: 600;
  line-height: 20px;
}

.dsh-queue-plus-sorting-label::before {
  margin-inline-end: 8px;
  color: var(--dsw-alias-label-tertiary);
  content: '·';
}

.dsh-queue-plus-mode-button,
.dsh-queue-plus-collapse-button,
.dsh-queue-plus-icon-button,
.dsh-queue-plus-inline-button,
.dsh-queue-plus-danger-button,
.dsh-queue-plus-clear-button,
.dsh-queue-plus-undo-button {
  font-family: Inter, var(--dsw-font-family);
  touch-action: manipulation;
  -webkit-tap-highlight-color: transparent;
}

.dsh-queue-plus-mode-button {
  flex: none;
  min-width: 48px;
  height: 30px;
  padding: 0 10px;
  color: var(--dsw-alias-label-secondary);
  font-size: 12px;
  font-weight: 500;
  line-height: 28px;
  cursor: pointer;
  background: transparent;
  border: 0;
  border-radius: 8px;
}

.dsh-queue-plus-mode-button[data-active='true'] {
  color: var(--dsw-alias-state-business-primary);
  background: var(--dsw-alias-interactive-bg-hover);
}

.dsh-queue-plus-collapse-button,
.dsh-queue-plus-icon-button {
  display: grid;
  flex: none;
  width: 30px;
  height: 30px;
  padding: 0;
  color: var(--dsw-alias-label-tertiary);
  cursor: pointer;
  background: transparent;
  border: 0;
  border-radius: 999px;
  place-items: center;
}

.dsh-queue-plus-mode-button:disabled,
.dsh-queue-plus-collapse-button:disabled,
.dsh-queue-plus-icon-button:disabled,
.dsh-queue-plus-inline-button:disabled,
.dsh-queue-plus-danger-button:disabled,
.dsh-queue-plus-clear-button:disabled,
.dsh-queue-plus-undo-button:disabled {
  cursor: default;
  opacity: .42;
}

.dsh-queue-plus-body {
  border-top: 1px solid var(--dsw-alias-border-l1);
}

.dsh-queue-plus-list {
  max-height: min(36dvh, 260px);
  margin: 0;
  padding: 0;
  overflow-y: auto;
  overscroll-behavior: contain;
  list-style: none;
}

.dsh-queue-plus-body[data-sorting='true'] .dsh-queue-plus-list {
  box-shadow: inset 2px 0 0 var(--dsw-alias-state-business-primary);
}

.dsh-queue-plus-body[data-drag-active='true'] {
  user-select: none;
}

.dsh-queue-plus-row {
  box-sizing: border-box;
  display: flex;
  align-items: center;
  width: 100%;
  min-height: 40px;
  gap: 10px;
  padding: 4px 6px 4px 12px;
  border-radius: 8px;
  content-visibility: auto;
  contain-intrinsic-size: auto 40px;
}

.dsh-queue-plus-row[data-mode='sort'] {
  display: grid;
  grid-template-columns: 24px 28px minmax(0, 1fr) 66px;
  gap: 6px;
  padding-inline-start: 6px;
}

.dsh-queue-plus-row + .dsh-queue-plus-row {
  box-shadow: inset 0 1px 0 var(--dsw-alias-border-l1);
}

.dsh-queue-plus-row[data-dragging='true'] {
  opacity: .48;
}

.dsh-queue-plus-row[data-dragover='true'] {
  background: var(--dsw-alias-interactive-bg-hover);
  box-shadow: inset 3px 0 0 var(--dsw-alias-state-business-primary);
}

.dsh-queue-plus-index {
  color: var(--dsw-alias-label-tertiary);
  font: 600 11px/20px ui-monospace, SFMono-Regular, Consolas, monospace;
  text-align: end;
  font-variant-numeric: tabular-nums;
}

.dsh-queue-plus-drag-handle {
  display: grid;
  width: 28px;
  height: 30px;
  color: var(--dsw-alias-label-tertiary);
  cursor: grab;
  border-radius: 8px;
  place-items: center;
  user-select: none;
  touch-action: manipulation;
}

.dsh-queue-plus-drag-handle:active {
  cursor: grabbing;
}

.dsh-queue-plus-grip-dots {
  position: relative;
  width: 3px;
  height: 3px;
  transform: translate(-3px, -6px);
  color: currentColor;
  background: currentColor;
  border-radius: 50%;
  box-shadow: 0 6px currentColor, 0 12px currentColor, 6px 0 currentColor, 6px 6px currentColor, 6px 12px currentColor;
}

.dsh-queue-plus-preview,
.dsh-queue-plus-editor {
  flex: 1 1 auto;
  min-width: 0;
  font: var(--dsw-font-xs-13);
  font-family: Inter, var(--dsw-font-family);
}

.dsh-queue-plus-preview {
  overflow: hidden;
  color: var(--dsw-alias-label-primary-dimmed);
  text-overflow: ellipsis;
  white-space: nowrap;
  word-break: break-word;
}

.dsh-queue-plus-editor {
  box-sizing: border-box;
  height: 30px;
  padding: 0 8px;
  color: var(--dsw-alias-label-primary);
  background: var(--dsw-alias-bg-base);
  border: 1px solid var(--dsw-alias-border-l2);
  border-radius: 7px;
  outline: none;
}

.dsh-queue-plus-editor:focus-visible {
  border-color: var(--dsw-alias-state-business-primary);
  box-shadow: 0 0 0 1px var(--dsw-alias-state-business-primary);
}

.dsh-queue-plus-actions {
  display: flex;
  flex: none;
  align-items: center;
  justify-content: flex-end;
  gap: 3px;
}

.dsh-queue-plus-inline-button,
.dsh-queue-plus-danger-button {
  flex: none;
  min-height: 30px;
  padding: 3px 9px;
  color: var(--dsw-alias-label-secondary);
  font-size: 12px;
  font-weight: 500;
  line-height: 20px;
  cursor: pointer;
  background: transparent;
  border: 0;
  border-radius: 8px;
}

.dsh-queue-plus-danger-button {
  color: var(--dsw-alias-state-error-primary);
}

.dsh-queue-plus-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  min-height: 40px;
  gap: 12px;
  padding: 4px 8px 4px 12px;
  border-top: 1px solid var(--dsw-alias-border-l1);
}

.dsh-queue-plus-footnote {
  min-width: 0;
  overflow: hidden;
  color: var(--dsw-alias-label-tertiary);
  font-size: 11px;
  line-height: 18px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.dsh-queue-plus-clear-button {
  flex: none;
  min-height: 30px;
  padding: 4px 9px;
  color: var(--dsw-alias-state-error-primary);
  font-size: 12px;
  font-weight: 500;
  line-height: 20px;
  cursor: pointer;
  background: transparent;
  border: 0;
  border-radius: 8px;
}

.dsh-queue-plus-undo {
  display: flex;
  align-items: center;
  min-height: 42px;
  gap: 10px;
  padding: 4px 8px 4px 12px;
}

.dsh-queue-plus-undo[data-attached='true'] {
  border-top: 1px solid var(--dsw-alias-border-l1);
}

.dsh-queue-plus-undo-text {
  flex: 1;
  min-width: 0;
  overflow: hidden;
  color: var(--dsw-alias-label-secondary);
  font-size: 12px;
  line-height: 20px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.dsh-queue-plus-undo-button {
  flex: none;
  min-height: 30px;
  padding: 3px 10px;
  color: var(--dsw-alias-state-business-primary);
  font-size: 12px;
  font-weight: 600;
  line-height: 20px;
  cursor: pointer;
  background: transparent;
  border: 1px solid var(--dsw-alias-state-business-primary);
  border-radius: 8px;
  font-variant-numeric: tabular-nums;
}

.dsh-queue-plus-notice {
  padding: 5px 12px 7px;
  color: var(--dsw-alias-label-secondary);
  font-size: 11px;
  line-height: 16px;
  border-top: 1px solid var(--dsw-alias-border-l1);
}

.dsh-queue-plus-notice[data-level='error'] {
  color: var(--dsw-alias-state-error-primary);
}

.dsh-queue-plus-sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  overflow: hidden;
  white-space: nowrap;
  border: 0;
  clip: rect(0, 0, 0, 0);
  clip-path: inset(50%);
}

.dsh-queue-plus-summary-button:focus-visible,
.dsh-queue-plus-mode-button:focus-visible,
.dsh-queue-plus-collapse-button:focus-visible,
.dsh-queue-plus-icon-button:focus-visible,
.dsh-queue-plus-inline-button:focus-visible,
.dsh-queue-plus-danger-button:focus-visible,
.dsh-queue-plus-clear-button:focus-visible,
.dsh-queue-plus-undo-button:focus-visible {
  outline: 2px solid var(--dsw-alias-state-business-primary);
  outline-offset: -2px;
}

@media (hover: hover) {
  .dsh-queue-plus-summary-button:hover:not(:disabled),
  .dsh-queue-plus-mode-button:hover:not(:disabled),
  .dsh-queue-plus-collapse-button:hover:not(:disabled),
  .dsh-queue-plus-icon-button:hover:not(:disabled),
  .dsh-queue-plus-inline-button:hover:not(:disabled),
  .dsh-queue-plus-danger-button:hover:not(:disabled),
  .dsh-queue-plus-clear-button:hover:not(:disabled),
  .dsh-queue-plus-undo-button:hover:not(:disabled),
  .dsh-queue-plus-drag-handle:hover {
    background: var(--dsw-alias-interactive-bg-hover);
  }
}

@media (pointer: coarse) {
  .dsh-queue-plus-header,
  .dsh-queue-plus-row,
  .dsh-queue-plus-undo {
    min-height: 48px;
  }

  .dsh-queue-plus-mode-button,
  .dsh-queue-plus-collapse-button,
  .dsh-queue-plus-icon-button,
  .dsh-queue-plus-inline-button,
  .dsh-queue-plus-danger-button,
  .dsh-queue-plus-drag-handle,
  .dsh-queue-plus-clear-button,
  .dsh-queue-plus-undo-button {
    min-width: 44px;
    min-height: 44px;
  }

  .dsh-queue-plus-row[data-mode='sort'] {
    grid-template-columns: 24px 44px minmax(0, 1fr) 92px;
  }
}

@media (max-width: 520px) {
  .dsh-queue-plus-dock {
    width: calc(100% - var(--dsh-composer-side-clearance) - var(--dsh-composer-side-clearance));
    padding-inline: 0;
  }

  .dsh-queue-plus-header {
    padding-inline-start: 2px;
  }

  .dsh-queue-plus-sorting-label {
    display: none;
  }

  .dsh-queue-plus-row {
    gap: 6px;
    padding-inline: 8px 4px;
  }

  .dsh-queue-plus-row[data-mode='sort'] {
    padding-inline-start: 4px;
  }
}

`;
		function installStyles(document) {
			const styleDocument = document;
			let element = document.getElementById(STYLE_ID);
			if (element === null) {
				element = document.createElement("style");
				element.id = STYLE_ID;
				element.textContent = STYLE_TEXT;
				document.head.appendChild(element);
			}
			styleDocument[STYLE_REFS] = (styleDocument[STYLE_REFS] ?? 0) + 1;
			let active = true;
			return () => {
				if (!active) return;
				active = false;
				const next = Math.max(0, (styleDocument[STYLE_REFS] ?? 1) - 1);
				styleDocument[STYLE_REFS] = next;
				if (next === 0) document.getElementById(STYLE_ID)?.remove();
			};
		}
		//#endregion
		//#region src/client/QueuePlusDock.tsx
		function queued(rows) {
			return rows.filter((row) => row.placement === "queued");
		}
		function ids(rows) {
			return rows.map((row) => String(row.id));
		}
		function sameOrder(left, right) {
			return left.length === right.length && left.every((id, index) => id === right[index]);
		}
		function sameMembers(left, right) {
			return left.length === right.length && new Set(left).size === left.length && left.every((id) => right.includes(id));
		}
		function projectOrder(rows, order) {
			if (order === null || !sameMembers(ids(rows), order)) return [...rows];
			const byId = new Map(rows.map((row) => [String(row.id), row]));
			const projected = order.map((id) => byId.get(id));
			return projected.every((row) => row !== void 0) ? projected : [...rows];
		}
		function errorText(error, t, fallback) {
			if (error instanceof QueuePlusClientError) {
				if ([
					"QUEUE_CHANGED",
					"ITEM_NOT_FOUND",
					"EMPTY_QUEUE",
					"INVALID_TARGET"
				].includes(error.code)) return t("error.queueChanged");
				if (error.code === "UNDO_EXPIRED" || error.code === "UNDO_NOT_FOUND") return t("error.expired");
				if ([
					"UNDO_CONFLICT",
					"UNDO_IN_PROGRESS",
					"AGENT_CHANGED"
				].includes(error.code)) return t("error.undoConflict");
			}
			return fallback ?? t("error.generic");
		}
		/** One queue surface with an in-place management/sorting mode switch. */
		function QueuePlusDock({ useSession, sessionId, updateQueue, t }) {
			const inbox = useSession((snapshot) => snapshot.queue);
			const queue = (0, react.useMemo)(() => queued(inbox), [inbox]);
			const running = useSession((snapshot) => snapshot.running);
			const queueMutable = useSession((snapshot) => snapshot.subagent === null);
			const [collapsed, setCollapsed] = (0, react.useState)(true);
			const [sorting, setSorting] = (0, react.useState)(false);
			const [editing, setEditing] = (0, react.useState)(null);
			const [confirmingRemove, setConfirmingRemove] = (0, react.useState)(null);
			const [busy, setBusy] = (0, react.useState)(null);
			const [undo, setUndo] = (0, react.useState)(null);
			const [clock, setClock] = (0, react.useState)(Date.now());
			const [notice, setNotice] = (0, react.useState)(null);
			const [announcement, setAnnouncement] = (0, react.useState)(null);
			const [pendingOrder, setPendingOrder] = (0, react.useState)(null);
			const [dragging, setDragging] = (0, react.useState)(null);
			const [dragOver, setDragOver] = (0, react.useState)(null);
			const listId = (0, react.useId)();
			const displayQueue = (0, react.useMemo)(() => projectOrder(queue, pendingOrder), [pendingOrder, queue]);
			(0, react.useEffect)(() => {
				if (pendingOrder === null) return;
				const liveOrder = ids(queue);
				if (sameOrder(liveOrder, pendingOrder) || !sameMembers(liveOrder, pendingOrder)) setPendingOrder(null);
			}, [pendingOrder, queue]);
			(0, react.useEffect)(() => {
				if (queue.length === 0) setCollapsed(true);
				if (queue.length < 2 || !queueMutable) setSorting(false);
				if (editing !== null && (!queueMutable || !queue.some((row) => row.id === editing.id))) setEditing(null);
				if (confirmingRemove !== null && (!queueMutable || !queue.some((row) => row.id === confirmingRemove))) setConfirmingRemove(null);
				if (dragging !== null && !queue.some((row) => String(row.id) === dragging)) setDragging(null);
				if (dragOver !== null && !queue.some((row) => String(row.id) === dragOver)) setDragOver(null);
			}, [
				confirmingRemove,
				dragOver,
				dragging,
				editing,
				queue,
				queueMutable
			]);
			(0, react.useEffect)(() => {
				if (confirmingRemove === null) return void 0;
				const timer = window.setTimeout(() => setConfirmingRemove(null), 5e3);
				return () => window.clearTimeout(timer);
			}, [confirmingRemove]);
			(0, react.useEffect)(() => {
				if (undo === null) return void 0;
				setClock(Date.now());
				const timer = window.setInterval(() => {
					const now = Date.now();
					setClock(now);
					if (now >= undo.expiresAt) {
						window.clearInterval(timer);
						setUndo((current) => current?.token === undo.token ? null : current);
					}
				}, 250);
				return () => window.clearInterval(timer);
			}, [undo]);
			(0, react.useEffect)(() => {
				if (notice === null) return void 0;
				const timer = window.setTimeout(() => setNotice((current) => current === notice ? null : current), 4e3);
				return () => window.clearTimeout(timer);
			}, [notice]);
			if (queue.length === 0 && undo === null && notice === null) return null;
			const interactionActive = busy !== null || editing !== null || confirmingRemove !== null;
			const listVisible = displayQueue.length === 1 || !collapsed || sorting || editing !== null;
			const seconds = undo === null ? 0 : Math.max(0, Math.ceil((undo.expiresAt - clock) / 1e3));
			const expectedOrder = () => ids(displayQueue);
			const announcePosition = (position) => {
				setAnnouncement((current) => ({
					key: (current?.key ?? 0) + 1,
					text: t("status.position", { position })
				}));
			};
			const move = async (itemId, toIndex) => {
				if (busy !== null || toIndex < 0 || toIndex >= displayQueue.length) return;
				const order = expectedOrder();
				if (order[toIndex] === itemId) return;
				setBusy("move");
				setNotice(null);
				try {
					const result = await moveQueue({
						action: "move",
						sessionId: String(sessionId),
						itemId,
						toIndex,
						expectedOrder: order
					});
					setPendingOrder(result.order);
					announcePosition(toIndex + 1);
				} catch (error) {
					setPendingOrder(null);
					setNotice({
						level: "error",
						text: errorText(error, t)
					});
				} finally {
					setBusy(null);
					setDragging(null);
					setDragOver(null);
				}
			};
			const applyQueueAction = async (itemId, action, failure) => {
				if (busy !== null) return false;
				setBusy("queue");
				setNotice(null);
				try {
					await updateQueue(itemId, action);
					return true;
				} catch (error) {
					setNotice({
						level: "error",
						text: errorText(error, t, failure)
					});
					return false;
				} finally {
					setBusy(null);
				}
			};
			const saveEdit = async () => {
				if (editing === null || editing.text.trim() === "") return;
				if (await applyQueueAction(editing.id, {
					kind: "edit",
					content: [{
						type: "text",
						text: editing.text
					}]
				}, t("error.edit"))) setEditing(null);
			};
			const removeMessage = async (itemId) => {
				if (await applyQueueAction(itemId, { kind: "remove" }, t("error.remove"))) setConfirmingRemove(null);
			};
			const clearAll = async () => {
				if (busy !== null || displayQueue.length === 0) return;
				setBusy("clear");
				setNotice(null);
				try {
					const result = await clearQueue(String(sessionId), expectedOrder());
					setUndo({
						token: result.token,
						count: result.count,
						expiresAt: result.expiresAt
					});
					setPendingOrder(null);
					setEditing(null);
					setConfirmingRemove(null);
					setSorting(false);
					setCollapsed(true);
				} catch (error) {
					setNotice({
						level: "error",
						text: errorText(error, t)
					});
				} finally {
					setBusy(null);
				}
			};
			const restore = async () => {
				if (busy !== null || undo === null) return;
				const current = undo;
				setBusy("undo");
				setNotice(null);
				try {
					const result = await undoClear(String(sessionId), current.token);
					setUndo(null);
					setCollapsed(false);
					setNotice({
						level: "info",
						text: t("status.restored", { count: result.restored })
					});
				} catch (error) {
					if (error instanceof QueuePlusClientError && (error.code === "UNDO_EXPIRED" || error.code === "UNDO_NOT_FOUND")) setUndo(null);
					setNotice({
						level: "error",
						text: errorText(error, t)
					});
				} finally {
					setBusy(null);
				}
			};
			const toggleSorting = () => {
				if (sorting) {
					setSorting(false);
					return;
				}
				setCollapsed(false);
				setNotice(null);
				setSorting(true);
			};
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
				className: styles.dock,
				"data-queue-plus-dock": "",
				"data-queue-plus-mode": sorting ? "sort" : "manage",
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("section", {
					className: styles.panel,
					"data-sorting": sorting || void 0,
					"aria-label": t("panel.label"),
					children: [
						displayQueue.length > 1 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("header", {
							className: styles.header,
							children: [
								/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
									type: "button",
									className: styles.summaryButton,
									"aria-expanded": listVisible,
									"aria-controls": listId,
									disabled: interactionActive || sorting,
									onClick: () => setCollapsed((value) => !value),
									children: [
										/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
											className: styles.lead,
											"aria-hidden": "true",
											children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconQueueOutline14, {})
										}),
										/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
											className: styles.count,
											children: t("panel.count", { count: displayQueue.length })
										}),
										sorting && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
											className: styles.sortingLabel,
											children: t("panel.sorting")
										})
									]
								}),
								queueMutable && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
									type: "button",
									className: styles.modeButton,
									"data-active": sorting || void 0,
									"aria-pressed": sorting,
									disabled: busy !== null || editing !== null || confirmingRemove !== null,
									onClick: toggleSorting,
									children: sorting ? t("action.finishSort") : t("action.sort")
								}),
								!sorting && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
									type: "button",
									className: styles.collapseButton,
									"aria-label": listVisible ? t("action.collapse") : t("action.expand"),
									"aria-expanded": listVisible,
									"aria-controls": listId,
									disabled: interactionActive,
									onClick: () => setCollapsed((value) => !value),
									children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										"aria-hidden": "true",
										children: listVisible ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconChevronDownOutline14, {}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconChevronUpOutline14, {})
									})
								})
							]
						}),
						displayQueue.length > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
							id: listId,
							className: styles.body,
							"data-sorting": sorting || void 0,
							"data-drag-active": dragging !== null || void 0,
							hidden: !listVisible,
							children: [listVisible && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("ol", {
								className: styles.list,
								"aria-label": sorting ? t("panel.sortList") : t("panel.list"),
								children: displayQueue.map((row, index) => {
									const itemId = String(row.id);
									const rowEditing = editing?.id === row.id;
									const rowConfirmingRemove = confirmingRemove === row.id;
									const otherEditActive = editing !== null && !rowEditing;
									return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("li", {
										className: styles.row,
										"data-mode": sorting ? "sort" : "manage",
										"data-dragging": dragging === itemId || void 0,
										"data-dragover": dragOver === itemId || void 0,
										onDragEnter: () => {
											if (sorting && dragging !== null) setDragOver(itemId);
										},
										onDragOver: (event) => {
											if (!sorting || dragging === null) return;
											event.preventDefault();
											event.dataTransfer.dropEffect = "move";
										},
										onDrop: (event) => {
											if (!sorting) return;
											event.preventDefault();
											const source = dragging ?? event.dataTransfer.getData("text/plain");
											if (source !== "" && source !== itemId) move(source, index);
										},
										children: sorting ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
											/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
												className: styles.index,
												children: String(index + 1).padStart(2, "0")
											}),
											/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
												className: styles.dragHandle,
												draggable: busy === null,
												"aria-hidden": "true",
												title: t("action.drag", { position: index + 1 }),
												onDragStart: (event) => {
													setDragging(itemId);
													event.dataTransfer.effectAllowed = "move";
													event.dataTransfer.setData("text/plain", itemId);
												},
												onDragEnd: () => {
													setDragging(null);
													setDragOver(null);
												},
												children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", { className: styles.gripDots })
											}),
											/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
												className: styles.preview,
												title: row.preview,
												children: row.preview
											}),
											/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
												className: styles.actions,
												children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.Tooltip, {
													label: t("action.moveUp", { position: index + 1 }),
													side: "bottom",
													delayMs: 500,
													children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
														type: "button",
														className: styles.iconButton,
														"aria-label": t("action.moveUp", { position: index + 1 }),
														disabled: busy !== null || index === 0,
														onClick: () => {
															move(itemId, index - 1);
														},
														children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
															"aria-hidden": "true",
															children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconChevronUpOutline14, {})
														})
													})
												}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.Tooltip, {
													label: t("action.moveDown", { position: index + 1 }),
													side: "bottom",
													delayMs: 500,
													children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
														type: "button",
														className: styles.iconButton,
														"aria-label": t("action.moveDown", { position: index + 1 }),
														disabled: busy !== null || index === displayQueue.length - 1,
														onClick: () => {
															move(itemId, index + 1);
														},
														children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
															"aria-hidden": "true",
															children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconChevronDownOutline14, {})
														})
													})
												})]
											})
										] }) : /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
											displayQueue.length === 1 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
												className: styles.lead,
												"aria-hidden": "true",
												children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconQueueOutline14, {})
											}),
											rowEditing ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
												autoFocus: true,
												className: styles.editor,
												"aria-label": t("action.edit"),
												autoComplete: "off",
												name: "queued-message",
												value: editing.text,
												onChange: (event) => setEditing({
													id: editing.id,
													text: event.currentTarget.value
												}),
												onKeyDown: (event) => {
													if (event.key === "Escape") {
														setEditing(null);
														return;
													}
													if (event.key === "Enter" && !event.nativeEvent.isComposing) {
														event.preventDefault();
														saveEdit();
													}
												}
											}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
												className: styles.preview,
												title: row.preview,
												children: row.preview
											}),
											queueMutable && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
												className: styles.actions,
												children: rowEditing ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.Tooltip, {
													label: t("action.save"),
													side: "bottom",
													delayMs: 500,
													children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
														type: "button",
														className: styles.iconButton,
														"aria-label": t("action.save"),
														disabled: busy !== null || editing.text.trim() === "",
														onClick: () => {
															saveEdit();
														},
														children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
															"aria-hidden": "true",
															children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconCheckOutline16, { size: 14 })
														})
													})
												}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.Tooltip, {
													label: t("action.cancelEdit"),
													side: "bottom",
													delayMs: 500,
													children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
														type: "button",
														className: styles.iconButton,
														"aria-label": t("action.cancelEdit"),
														disabled: busy !== null,
														onClick: () => setEditing(null),
														children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
															"aria-hidden": "true",
															children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconCloseOutline16, { size: 14 })
														})
													})
												})] }) : rowConfirmingRemove ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
													type: "button",
													className: styles.inlineButton,
													disabled: busy !== null,
													onClick: () => setConfirmingRemove(null),
													children: t("action.cancelRemove")
												}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
													type: "button",
													className: styles.dangerButton,
													disabled: busy !== null,
													onClick: () => {
														removeMessage(row.id);
													},
													children: t("action.confirmRemove")
												})] }) : /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
													/* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.Tooltip, {
														label: t("action.edit"),
														side: "bottom",
														delayMs: 500,
														disabled: row.text === null,
														children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
															type: "button",
															className: styles.iconButton,
															"aria-label": t("action.edit"),
															title: row.text === null ? t("action.editUnsupported") : void 0,
															disabled: busy !== null || otherEditActive || confirmingRemove !== null || row.text === null,
															onClick: () => {
																if (row.text !== null) setEditing({
																	id: row.id,
																	text: row.text
																});
															},
															children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
																"aria-hidden": "true",
																children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconEditOutline16, { size: 14 })
															})
														})
													}),
													/* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.Tooltip, {
														label: t("action.remove"),
														side: "bottom",
														delayMs: 500,
														children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
															type: "button",
															className: styles.iconButton,
															"aria-label": t("action.remove"),
															disabled: busy !== null || editing !== null || confirmingRemove !== null,
															onClick: () => {
																setNotice(null);
																setConfirmingRemove(row.id);
															},
															children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
																"aria-hidden": "true",
																children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconTrashOutline16, { size: 14 })
															})
														})
													}),
													/* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.Tooltip, {
														label: t("action.steer"),
														side: "bottom",
														delayMs: 500,
														disabled: !running,
														children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
															type: "button",
															className: styles.iconButton,
															"aria-label": t("action.steer"),
															title: running ? void 0 : t("action.steerUnavailable"),
															disabled: busy !== null || editing !== null || confirmingRemove !== null || !running,
															onClick: () => {
																applyQueueAction(row.id, { kind: "steer" }, t("error.steer"));
															},
															children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
																"aria-hidden": "true",
																children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconSendOutline14, {})
															})
														})
													})
												] })
											})
										] })
									}, itemId);
								})
							}), listVisible && queueMutable && displayQueue.length > 1 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("footer", {
								className: styles.footer,
								children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: styles.footnote,
									children: sorting ? t("hint.sort") : t("hint.undo")
								}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
									type: "button",
									className: styles.clearButton,
									disabled: busy !== null || editing !== null || confirmingRemove !== null,
									onClick: () => {
										clearAll();
									},
									children: t("action.clear")
								})]
							})]
						}),
						undo !== null && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
							className: styles.undo,
							"data-attached": displayQueue.length > 0 || void 0,
							role: "status",
							"aria-live": "polite",
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: styles.undoText,
								children: t("status.cleared", { count: undo.count })
							}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
								type: "button",
								className: styles.undoButton,
								disabled: busy !== null || seconds === 0,
								onClick: () => {
									restore();
								},
								children: t("action.undo", { seconds })
							})]
						}),
						notice !== null && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
							className: styles.notice,
							"data-level": notice.level,
							role: notice.level === "error" ? "alert" : "status",
							"aria-live": notice.level === "error" ? "assertive" : "polite",
							children: notice.text
						}),
						announcement !== null && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: styles.srOnly,
							role: "status",
							"aria-live": "polite",
							children: announcement.text
						}, announcement.key)
					]
				})
			});
		}
		//#endregion
		//#region src/client/locales.ts
		const NS = "queue-plus";
		const zh = {
			"panel.label": "排队消息",
			"panel.count": "{count} 条排队消息",
			"panel.sorting": "排序中",
			"panel.list": "排队消息列表",
			"panel.sortList": "按执行顺序排列的消息",
			"action.expand": "展开排队消息",
			"action.collapse": "收起排队消息",
			"action.sort": "排序",
			"action.finishSort": "完成",
			"action.edit": "编辑消息",
			"action.editUnsupported": "含有非文本内容，暂不支持编辑",
			"action.save": "保存修改",
			"action.cancelEdit": "取消编辑",
			"action.remove": "删除消息",
			"action.cancelRemove": "取消",
			"action.confirmRemove": "确认删除",
			"action.steer": "插话发送",
			"action.steerUnavailable": "当前没有正在执行的回答",
			"action.drag": "拖动第 {position} 条消息调整顺序",
			"action.moveUp": "上移第 {position} 条消息",
			"action.moveDown": "下移第 {position} 条消息",
			"action.clear": "清空全部",
			"action.undo": "撤销（{seconds}s）",
			"status.cleared": "已清空 {count} 条排队消息",
			"status.restored": "已恢复 {count} 条排队消息",
			"status.position": "已移至第 {position} 位",
			"hint.sort": "拖动手柄，或使用上下箭头",
			"hint.undo": "清空后 10 秒内可撤销",
			"error.queueChanged": "队列刚刚发生变化，请按最新顺序重试。",
			"error.expired": "撤销时间已过，已清空的消息无法恢复。",
			"error.undoConflict": "队列已发生变化，暂时无法恢复这些消息。",
			"error.edit": "修改失败，请重试。",
			"error.remove": "删除失败，请重试。",
			"error.steer": "插话发送失败，请重试。",
			"error.generic": "操作失败，请稍后重试。"
		};
		const en = {
			"panel.label": "Queued messages",
			"panel.count": "{count} queued messages",
			"panel.sorting": "Sorting",
			"panel.list": "Queued messages",
			"panel.sortList": "Messages in run order",
			"action.expand": "Expand queued messages",
			"action.collapse": "Collapse queued messages",
			"action.sort": "Sort",
			"action.finishSort": "Done",
			"action.edit": "Edit message",
			"action.editUnsupported": "Messages with non-text content cannot be edited",
			"action.save": "Save changes",
			"action.cancelEdit": "Cancel editing",
			"action.remove": "Remove message",
			"action.cancelRemove": "Cancel",
			"action.confirmRemove": "Remove",
			"action.steer": "Send as steering",
			"action.steerUnavailable": "There is no running response to steer",
			"action.drag": "Drag message {position} to reorder",
			"action.moveUp": "Move message {position} up",
			"action.moveDown": "Move message {position} down",
			"action.clear": "Clear all",
			"action.undo": "Undo ({seconds}s)",
			"status.cleared": "Cleared {count} queued messages",
			"status.restored": "Restored {count} queued messages",
			"status.position": "Moved to position {position}",
			"hint.sort": "Drag the handle, or use the arrow buttons",
			"hint.undo": "Clear all can be undone for 10 seconds",
			"error.queueChanged": "The queue just changed. Retry from the latest order.",
			"error.expired": "The undo window expired; cleared messages cannot be restored.",
			"error.undoConflict": "The queue changed, so these messages cannot be restored right now.",
			"error.edit": "The message could not be updated. Try again.",
			"error.remove": "The message could not be removed. Try again.",
			"error.steer": "The message could not be sent as steering. Try again.",
			"error.generic": "The operation failed. Try again."
		};
		//#endregion
		//#region src/client/index.ts
		const inject = [
			"slots",
			"locale",
			"conversation",
			"sessions"
		];
		function apply(ctx) {
			const sessions = ctx.sessions;
			ctx.effect(() => installStyles(document), "queue-plus: styles");
			ctx.effect(() => ctx.locale.register(NS, {
				zh,
				en
			}), "queue-plus: dictionaries");
			ctx.slots.inject("conversation.input.dock", () => ctx.slots.register({
				name: "conversation.input.dock",
				id: "queue",
				order: 20,
				priority: -10,
				locale: NS,
				inject: (sessionId) => {
					const actx = sessions.scope(sessionId);
					if (actx === void 0) throw new Error(`queue plus: session "${sessionId}" resolved no scope`);
					const conversation = actx.get("conversation");
					if (conversation === void 0) throw new Error("queue plus: conversation service unavailable");
					return { updateQueue: (itemId, action) => conversation.updateQueue(itemId, action) };
				}
			}, (props) => (0, react.createElement)(QueuePlusDock, props)));
		}
		//#endregion
		exports.QueuePlusDock = QueuePlusDock;
		exports.apply = apply;
		exports.inject = inject;
		return module.exports;
	}
});

//# sourceMappingURL=client.js.map