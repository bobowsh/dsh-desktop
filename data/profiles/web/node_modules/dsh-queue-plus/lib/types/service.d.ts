import type { Agent } from '@deepseek-ai/dsh-agent';
import { type QueuePlusRequest, type QueuePlusResult } from './shared';
/** Typed failure translated to a narrow HTTP response by the host entry. */
export declare class QueuePlusError extends Error {
    readonly status: number;
    readonly code: string;
    constructor(status: number, code: string, message: string);
}
export interface QueuePlusServiceOptions {
    resolveAgent(sessionId: string): Agent | undefined;
}
/** Decode the untrusted JSON body before it reaches the queue mutation layer. */
export declare function decodeQueuePlusRequest(value: unknown): QueuePlusRequest;
/** Host-only queue transaction service. Every operation re-reads live state. */
export declare class QueuePlusService {
    private readonly options;
    constructor(options: QueuePlusServiceOptions);
    execute(request: QueuePlusRequest): Promise<QueuePlusResult>;
    private agentFor;
    private assertExpected;
    private move;
}
//# sourceMappingURL=service.d.ts.map