# dsh bundle patch: inserts this plugin into a profile's layer stack.
# Merged plugin: 全局规则编辑 + 项目规则编辑 + 自动拆分（原 dsh-global-rules / dsh-rule-splitter）。
- insert:
    - id: rule-manager
      name: 'dsh-rule-manager'
