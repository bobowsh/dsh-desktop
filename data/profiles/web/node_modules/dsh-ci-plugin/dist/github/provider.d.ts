import type { CIArtifact, CIJob, CIRun, CIProviderCapabilities, DetectedRepository, ListRunsParams, GetRunParams, ListJobsParams, FailureEvidenceParams, LogExcerptParams, ListArtifactsParams, RerunParams, CancelParams, PaginatedResult, CILogExcerpt, CIFailureEvidence } from '../core/types';
import { AbstractCIProvider } from '../core/provider';
import type { CredentialProvider, ApprovalGate } from '../core/context';
export interface GitHubProviderConfig {
    apiBase?: string;
    userAgent?: string;
    maxRetries?: number;
    timeoutMs?: number;
}
export declare class CIError extends Error {
    readonly code: string;
    readonly httpStatus?: number | undefined;
    constructor(message: string, code: string, httpStatus?: number | undefined);
}
export declare class GitHubActionsProvider extends AbstractCIProvider {
    private readonly credentials;
    private readonly approval;
    private readonly apiBase;
    private readonly userAgent;
    private readonly maxRetries;
    private readonly timeoutMs;
    private cachedRepo;
    constructor(credentials: CredentialProvider, approval: ApprovalGate, config?: GitHubProviderConfig);
    capabilities(): CIProviderCapabilities;
    detectRepository(): Promise<DetectedRepository | null>;
    listRuns(params: ListRunsParams): Promise<PaginatedResult<CIRun>>;
    getRun(runId: string, params?: GetRunParams): Promise<CIRun | null>;
    listJobs(runId: string, params?: ListJobsParams): Promise<PaginatedResult<CIJob>>;
    getFailureEvidence(runId: string, params?: FailureEvidenceParams): Promise<CIFailureEvidence | null>;
    getLogExcerpt(params: LogExcerptParams): Promise<CILogExcerpt | null>;
    listArtifacts(runId: string, params?: ListArtifactsParams): Promise<PaginatedResult<CIArtifact>>;
    rerun(runId: string, params?: RerunParams): Promise<CIRun | null>;
    rerunFailed(runId: string, params?: RerunParams): Promise<CIRun | null>;
    cancel(runId: string, params?: CancelParams): Promise<boolean>;
    private getJobLog;
    private fetchJson;
    private fetchResponse;
}
//# sourceMappingURL=provider.d.ts.map