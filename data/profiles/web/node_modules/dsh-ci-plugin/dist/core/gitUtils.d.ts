import type { DetectedRepository } from './types';
export type GitContext = {
    remote: string;
    branch?: string;
    commit?: string;
};
/**
 * Gather git context from the current working directory.
 */
export declare function gatherGitContext(): Promise<GitContext | null>;
/**
 * Parse git remote URLs to extract owner/repo.
 */
export declare function parseGitRemote(url: string): DetectedRepository | null;
//# sourceMappingURL=gitUtils.d.ts.map