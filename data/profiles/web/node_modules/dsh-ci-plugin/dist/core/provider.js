export class AbstractCIProvider {
    async resolveRepository(params = {}) {
        if (params.repository)
            return params.repository;
        const repo = await this.detectRepository();
        return repo ? `${repo.owner}/${repo.repo}` : null;
    }
}
//# sourceMappingURL=provider.js.map