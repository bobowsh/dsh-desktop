export const DEFAULT_SECRET_PATTERNS = [
    {
        name: 'github-token',
        regex: /ghp_[A-Za-z0-9]{36}/g,
        replacement: '[REDACTED_GITHUB_TOKEN]',
    },
    {
        name: 'gitlab-token',
        regex: /glpat-[A-Za-z0-9\-]{20,}/g,
        replacement: '[REDACTED_GITLAB_TOKEN]',
    },
    {
        name: 'aws-access-key',
        regex: /AKIA[0-9A-Z]{16}/g,
        replacement: '[REDACTED_AWS_KEY]',
    },
    {
        name: 'generic-password',
        regex: /(password|passwd|secret|api[_-]?key|auth[_-]?token|access[_-]?token)\s*[:=]\s*['"]?([^\s'"&;]{8,})['"]?/gi,
        replacement: '$1: [REDACTED]',
    },
    {
        name: 'private-key',
        regex: /-----BEGIN\s+(RSA\s+|EC\s+|DSA\s+)?PRIVATE\s+KEY-----/g,
        replacement: '[REDACTED_PRIVATE_KEY]',
    },
    {
        name: 'generic-token',
        regex: /(?:Bearer\s+)[A-Za-z0-9\-._~+\/]+=*/g,
        replacement: 'Bearer [REDACTED]',
    },
];
export function redactSecrets(text, patterns = DEFAULT_SECRET_PATTERNS) {
    let result = text;
    for (const pattern of patterns) {
        result = result.replace(pattern.regex, pattern.replacement);
    }
    return result;
}
//# sourceMappingURL=security.js.map