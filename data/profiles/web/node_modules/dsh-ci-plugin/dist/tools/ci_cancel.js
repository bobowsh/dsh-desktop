export async function ci_cancel(context, params) {
    try {
        const success = await context.cancel(params.runId, params);
        return JSON.stringify({
            success,
            runId: params.runId,
            message: success ? 'Cancel requested successfully.' : 'Cancel could not be processed (run may have already completed).',
        });
    }
    catch (error) {
        return JSON.stringify({
            success: false,
            error: error instanceof Error ? error.message : 'Unknown error',
        });
    }
}
//# sourceMappingURL=ci_cancel.js.map