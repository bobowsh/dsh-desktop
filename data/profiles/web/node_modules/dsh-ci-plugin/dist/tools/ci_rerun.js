export async function ci_rerun(context, params) {
    try {
        const run = await context.rerun(params.runId, params);
        if (!run) {
            return JSON.stringify({
                success: false,
                error: 'Rerun failed or not found.',
            });
        }
        return JSON.stringify({
            success: true,
            dryRun: params.dryRun ?? false,
            runId: run.id,
            status: run.status,
            url: run.url,
            message: params.dryRun ? 'Dry run - no actual rerun performed.' : 'Rerun triggered successfully.',
        });
    }
    catch (error) {
        return JSON.stringify({
            success: false,
            error: error instanceof Error ? error.message : 'Unknown error',
        });
    }
}
//# sourceMappingURL=ci_rerun.js.map