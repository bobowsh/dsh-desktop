export async function ci_run(context, params) {
    try {
        const run = await context.getRun(params.runId, params);
        if (!run) {
            return JSON.stringify({ error: `Run ${params.runId} not found` });
        }
        return JSON.stringify({
            id: run.id,
            name: run.name,
            status: run.status,
            conclusion: run.conclusion,
            branch: run.headBranch,
            sha: run.headSha,
            url: run.url,
            createdAt: run.createdAt,
            updatedAt: run.updatedAt,
            jobsUrl: run.jobsUrl,
        });
    }
    catch (error) {
        return JSON.stringify({
            error: error instanceof Error ? error.message : 'Unknown error',
        });
    }
}
//# sourceMappingURL=ci_run.js.map