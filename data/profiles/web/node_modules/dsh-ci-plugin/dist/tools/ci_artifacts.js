export async function ci_artifacts(context, params) {
    try {
        const artifacts = await context.listArtifacts(params.runId, params);
        return JSON.stringify({
            total: artifacts.total,
            page: artifacts.page,
            perPage: artifacts.perPage,
            artifacts: artifacts.items.map(a => ({
                id: a.id,
                name: a.name,
                sizeInBytes: a.sizeInBytes,
                createdAt: a.createdAt,
                downloadUrl: a.downloadUrl,
            })),
        });
    }
    catch (error) {
        return JSON.stringify({
            error: error instanceof Error ? error.message : 'Unknown error',
        });
    }
}
//# sourceMappingURL=ci_artifacts.js.map