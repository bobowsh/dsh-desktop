import { ci_status } from './ci_status';
import { ci_runs } from './ci_runs';
import { ci_run } from './ci_run';
import { ci_jobs } from './ci_jobs';
import { ci_failure } from './ci_failure';
import { ci_log } from './ci_log';
import { ci_artifacts } from './ci_artifacts';
import { ci_rerun } from './ci_rerun';
import { ci_cancel } from './ci_cancel';
export { ci_status, ci_runs, ci_run, ci_jobs, ci_failure, ci_log, ci_artifacts, ci_rerun, ci_cancel, };
/**
 * Register all CI tools into a DSH plugin manifest. Returns a map of tool name to handler.
 */
export function registerTools(context) {
    return {
        ci_status: (params) => ci_status(context, params),
        ci_runs: (params) => ci_runs(context, params),
        ci_run: (params) => ci_run(context, params),
        ci_jobs: (params) => ci_jobs(context, params),
        ci_failure: (params) => ci_failure(context, params),
        ci_log: (params) => ci_log(context, params),
        ci_artifacts: (params) => ci_artifacts(context, params),
        ci_rerun: (params) => ci_rerun(context, params),
        ci_cancel: (params) => ci_cancel(context, params),
    };
}
//# sourceMappingURL=index.js.map