//#region lib/index.js
/**
 * workbuddy-hero — WorkBuddy 风格欢迎页插件（host 半边）。
 * 空的 apply 给 Loader 一个 host 侧行；浏览器半边经 `exports["./client"]` 下发。
 */
/** Host plugin body — this package contributes browser presentation only. */
function apply() {}
//#endregion
export { apply };
